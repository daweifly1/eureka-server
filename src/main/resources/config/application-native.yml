spring:
  application:
    name: center-server
  profiles:
    active: native
    include: admin,config,eureka,zipkin
  cloud:
    inetutils:
      preferred-networks:
        10.100
management:
  context-path: /cccc
server:
  port: 18001

spring:
  application:
    name: center-server
  profiles:
    active: peer3
    include: eureka3
  cloud:
    inetutils:
      preferred-networks:
        10.100
management:
  context-path: /cccc
server:
  port: 18003
